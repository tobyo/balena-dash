events{}
http{
  server {
    listen      80;
    server_name theolivers.org;

    # Allow only for register SSL (Certbot)
    location ^~ /.well-known/acme-challenge { 
      root /var/www/certbot;
    }
  }
}
